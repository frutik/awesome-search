name: build and deploy Awesome Search site

on:
  push:
    branches: [master]
    paths:
      - "obsidian/vault/**"
      - "fix-folder-pages.py"
      - "quartz-config/**"
      - ".github/workflows/build-and-deploy.yml"
  workflow_dispatch:
    inputs:
      deploy:
        description: "Also deploy web/ to GitHub Pages on this manual run"
        type: boolean
        default: false

# quartz/ is gitignored and not part of this repo (see CLAUDE.md), so every
# run scaffolds it fresh at the version the published site's footer reports
# (v4.5.2), then drops in the two config files this repo tracks under
# quartz-config/ — reverse-engineered from a diff against a stock v4.5.2
# build. See quartz-config/README.md.
#
# Builds to web/. Every push to master deploys it live via GitHub Pages'
# Actions source (Settings > Pages > Source = GitHub Actions). A manual
# workflow_dispatch only deploys if the deploy input is checked, so you can
# still trigger a build-only run on demand. docs/ / build-web.sh / release.sh
# are untouched and unused now that Pages reads from Actions instead.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Scaffold Quartz
        run: npx --yes degit jackyzha0/quartz#v4.5.2 quartz

      - name: Apply repo-tracked Quartz config
        run: |
          cp quartz-config/quartz.config.ts quartz/quartz.config.ts
          cp quartz-config/quartz.layout.ts quartz/quartz.layout.ts

      - name: Install Quartz dependencies
        run: npm ci
        working-directory: quartz

      - name: Build site to web/
        run: |
          cd quartz && npx quartz build --output ../web --directory ../obsidian/vault/Awesome\ Search && cd ..
          python3 fix-folder-pages.py web
          cp web/History.html web/index.html

      - name: Package web/ as a Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

      - name: Zip web/ for manual download
        # actions/upload-artifact validates every individual file path and
        # rejects characters like ">" — some note titles (e.g. "Hybrid
        # search -> sum of its parts") end up literally in generated
        # og-image filenames. Zipping first uploads one safely-named file
        # instead, sidestepping the per-entry check.
        run: cd web && zip -qr ../web.zip . && cd ..

      - name: Upload web/ as downloadable artifact
        uses: actions/upload-artifact@v4
        with:
          name: web
          path: web.zip
          retention-days: 14

  deploy:
    needs: build
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.deploy == true)
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
